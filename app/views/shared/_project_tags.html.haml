.tags.proj_tags
  %a.btn.btn-mini.disabled{:class => "#{project.project_type.delete(' ').downcase}"}
    ="#{project.project_type}"
  %a.btn.btn-mini.disabled{:class => "#{project.sector_color}"}
    ="#{project.sector}"